# DHCP-Simulation-and-TCP-Chat-Communication
This project demonstrates a Python-based simulation of the Dynamic Host Configuration Protocol (DHCP) along with a LAN client–server chat system. The DHCP module assigns IP addresses using the DORA  process with lease management, while the TCP chat enables realtime messaging between two devices on the same network.
